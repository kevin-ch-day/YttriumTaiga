# Repo-wide rules (sourced by Phase 01–03)
# Space-separated list of blocked team numbers.
CCDC_BLOCKED_TEAMS="19"

# Default time zone + display format for ops logging.
# Example: 1/21/2026 1:45 PM
CCDC_TIMEZONE="America/Chicago"
CCDC_TIME_FORMAT="%m/%d/%Y %l:%M %p"

# Central intel directory (relative to repo root if not absolute)
CCDC_INTEL_DIR="data/intel"
